# Copy this file to .sops.yaml and replace the placeholder with the public age
# recipient produced by: age-keygen -y < /path/to/keys.txt
# Never place an AGE-SECRET-KEY value in this file.
creation_rules:
  - path_regex: \.sops\.(yaml|json|env)$
    age: age1REPLACE_WITH_YOUR_PUBLIC_RECIPIENT
